def create_game(server_score, receiver_score):
    return TennisGame(server_score, receiver_score)


class TennisGame:    
    def __init__(self, server_score, receiver_score):
        self.game_over = False
        self.server_score = server_score
        self.receiver_score = receiver_score
        self.scores = [0,15,30,40]
        self.score = 

    def server_wins_point(self):
        if score = [0,0]
        #self.scores.index(self.server_score)